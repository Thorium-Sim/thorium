mutation UpdateSetClient(
  $id: ID!
  $clientId: ID!
  $secondary: Boolean
  $soundPlayer: Boolean
) {
  updateSetClient(
    id: $id
    client: {id: $clientId, secondary: $secondary, soundPlayer: $soundPlayer}
  )
}
