import React from "react";
import WrappedPowerDistribution from ".";
const PowerDistributionRightWing = (props: any) => (
  <WrappedPowerDistribution {...props} wing="right" />
);
export default PowerDistributionRightWing;
