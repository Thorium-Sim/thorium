subscription FabricationInventorySub($simulatorId: ID!) {
  inventoryUpdate(simulatorId: $simulatorId) {
    id
    name
    metadata {
      type
      description
      image
    }
    roomCount {
      room {
        id
      }
      count
    }
  }
}
