mutation CountermeasuresConfigureModule(
  $id: ID!
  $slot: CountermeasureSlotEnum!
  $moduleId: ID!
  $config: JSON!
) {
  countermeasuresConfigureModule(
    id: $id
    slot: $slot
    moduleId: $moduleId
    config: $config
  )
}
