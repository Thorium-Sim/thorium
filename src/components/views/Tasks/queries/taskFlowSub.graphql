subscription TaskFlowSub($simulatorId: ID) {
  taskFlows(simulatorId: $simulatorId) {
    id
    name
    category
    currentStep
    steps {
      id
      name
      activeTasks {
        id
        station
        definition
        verified
      }
      completed
    }
    completed
  }
}
