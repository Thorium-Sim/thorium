subscription LightingControl($simulatorId: ID!) {
  simulatorsUpdate(simulatorId: $simulatorId) {
    id
    lighting {
      intensity
      action
      actionStrength
      transitionDuration
      dmxConfig {
        id
        config
        actionStrength
      }
    }
    alertlevel
  }
}
